version: '3'

services:

  # ----------------
  # PostgreSQL.
  # ----------------
  server:
    build:
      context: .
      dockerfile: ./dockerfile
    tty: true

  server:
    build:
      context: .
      dockerfile: ./dockerfile
    tty: true
