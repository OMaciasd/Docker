version: '3'

services:

  # ----------------
  # PostgreSQL.
  # ----------------
  server:
    build:
      context: .
      dockerfile: ./dockerfile
    tty: true

  clien:
    build:
      context: .
      dockerfile: ./dockerfile
    tty: true
